Remove duplicates from sorted array in place and return number of unique elements.If k unique elements,then first k elements of the array should be unique.

**Approach:** Two pointer approach. Traverse the array using one pointer and modify the elements using other.

**Time Complexity:** O(n)

**Space Complexity:** O(1)
