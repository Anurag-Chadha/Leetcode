Given two string s and t. Return true if t is anagram(t can be formed by rearranging elements of s and using each element once.) of s.

**Optimal Approach:** Use hash array to keep track of occurrences of each character and check if they are anagram.

**Time Complexity:** O(n)

**Space Complexity:** O(1)
