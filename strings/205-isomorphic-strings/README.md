Given two strings. Return true if they are isomorphic, characters from one string can be mapped one-to-one to another string while preserving order.

**Optimal Approach:** Store the mappings in array or map, check if the current element was already mapped to a different element.

**Time Complexity:** O(n)

**Space Complexity:** O(1)
