Move zeroes to the end of the given array while maintaining relative order of the non zero elements

**Optimal Approach:** Two pointer approach, traverse the array using one pointer(ptr1) and use the other pointer(ptr2) to write non zero elements. All the elements before ptr2 are non zero.

**Time Complexity:** O(n)

**Space Complexity:** O(1)
