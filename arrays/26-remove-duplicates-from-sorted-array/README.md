Remove duplicates from a sorted array in place and return number of unique elements.If number of unique elements are k then first k elements of original array must be unique.

**Approach:** Two pointer approach.Use one pointer to traverse array and other to modify elements.

**Time Complexity:** O(n)

**Space Complexity:** O(1)
