Given an array find the element which appears >n/2 times.

**Optimal Approach:** Using Moore's Voting algorithm, majority element can't be cancelled in any case, it survives.

**Time Complexity:** O(n)

**Space Complexity:** O(1)
