All elements in array appear twice except for one. Find the single element.

**Optimal Approach:** XOR all elements of array, we get the single element as answer.

**Time Complexity:** O(n)

**Space Complexity:** O(1)
